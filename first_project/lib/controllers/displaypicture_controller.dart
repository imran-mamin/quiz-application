import 'package:get/get.dart';

class DisplayPictureController extends GetxController {
    // A flag that says whether a file chosen in File Explorer should be send to backend.
    RxBool send = true.obs;
}
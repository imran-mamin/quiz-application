import 'package:flutter/material.dart';

class Constants {
  // Max screen width contant to avoid repetition.
  static const maxScreenWidth = 800.0;
  static const appBarBackgroundColor = const Color.fromARGB(255, 27, 39, 93);
  static const canvasBackgroundColor = const Color.fromARGB(255, 110, 153, 222);
}